/* -FUNCTIONS-


Functions are blocks of code designed to perfrom a task, but they are executed when they are invoked (called).


Ex.
*/

var x = myFunction(50,2);

function myFunction( birthMonth, age){
    
    return birthMonth + age;
    
    
}

/*

-function keyword first, than function name, than (), inside the () goes the parameters, 
than the curly brackets with your block of code.

*/




